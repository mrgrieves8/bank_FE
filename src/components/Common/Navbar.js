// Navbar.js
import React from "react";

function Navbar() {
    return (
        <nav>
            {/* nav links here */}
        </nav>
    );
}

export default Navbar; // make sure it's exported as default
